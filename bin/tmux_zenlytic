#!/bin/zsh
SESSION="zenlytic"
FOLDER1="frontend"
FOLDER2="backend"
SERVER1="cd frontend; cleargulp serve"
SERVER2="cd backend; clearnode-inspector -p 8888 &;nodemon --debug ./bin/www"

tmux_dev_multi "$SESSION" "$FOLDER1" "$FOLDER2" "$SERVER1" "$SERVER2"
